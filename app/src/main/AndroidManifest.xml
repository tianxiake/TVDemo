<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yongjie.tvdemo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.SecondActivity" />
        <activity android:name=".activity.ThirdActivity" />
        <activity android:name=".activity.RxJavaTestActivity" />
        <activity android:name=".activity.EventBusTestMainActivity" />
        <activity android:name=".activity.EventBusTestSecondActivity" />
        <activity android:name=".activity.TestActivity" />
        <activity android:name=".activity.Test2Activity" />
        <activity android:name=".activity.ForthActivity"></activity>
    </application>

</manifest>